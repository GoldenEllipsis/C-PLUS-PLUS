对象的生存期


静态生存期

这种生存期与程序的运行期相同。
在文件作用域中声明的对象具有这种生存期。
在函数内部声明静态生存期对象，要冠以关键字static 。


动态生存期

块作用域中声明的，没有用static修饰的对象是动态生存期的对象（习惯称局部生存期对象）。
开始于程序执行到声明点时，结束于命名该标识符的作用域结束处。


静态局部变量在程序编译期间就已经进行初始化且只进行一次初始化，并且存储在静态存储区，
所以其生存期是整个程序，但他的作用域并不发生改变，仍旧在最内层包围他的函数体内。
